package com.example.recipeapp.domain;

public enum Difficulty {
    // TODO:
    //  1. provide proper name to be shown on the dropdown
    //  2. Experiment in recipeform. th:value="${difficultyValue.name()}" => th:value="${difficultyValue}"

    EASY,
    MODERATE,
    KIND_OF_HARD,
    HARD

}
